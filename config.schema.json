{
  "pluginAlias": "MATouchPlatform",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "pin": {
        "title": "PIN",
        "type": "integer",
        "required": true,
        "default": "0000",
        "description": "The same PIN must be used for all thermostats."
      }
    }
  }
}
